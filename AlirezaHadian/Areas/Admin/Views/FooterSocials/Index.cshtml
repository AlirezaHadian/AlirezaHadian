@model IEnumerable<AlirezaHadian.Models.FooterSocial>

@{
    ViewData["Title"] = "لینک های فوتر";
}

<a asp-action="Create" class="btn btn-success">
    افزودن لینک جدید
    <i class="uil uil-plus-circle"></i>
</a>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FooterIcon)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FooterLink)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <h3>
                    <i class="@item.FooterIcon"></i>
                </h3>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FooterLink)
            </td>
                <td class="pull-left">
                    <a asp-action="Edit" asp-route-id="@item.FooterSocialID" class="btn btn-primary btn-md">
                        <i class="uil uil-edit"></i>
                    </a>
                    <a href="/Admin/FooterSocials/Delete/@item.FooterSocialID" class="delete-link btn btn-danger btn-md">
                        <i class="uil uil-trash-alt"></i>
                    </a>
                </td>
        </tr>
}
    </tbody>
</table>

<script>
    // Attach a click event to the delete links/buttons
    const deleteLinks = document.querySelectorAll('.delete-link');
    deleteLinks.forEach(link => {
        link.addEventListener('click', function (e) {
            e.preventDefault();

            const href = this.getAttribute('href');

            Swal.fire({
                title: 'آیا از حذف مطمئن هستید؟',
                icon: 'warning',
                showCancelButton: true,
                cancelButtonText: 'لغو',
                confirmButtonColor: '#d33',
                cancelButtonColor: '#3085d6',
                confirmButtonText: 'بله'
            }).then((result) => {
                if (result.isConfirmed) {
                    window.location.href = href;
                    swal.fire({
                        icon: 'success',
                        title: "با موفقیت حذف شد!",
                        type: 'success',
                        showCancelButton: false,
                        showConfirmButton: false,
                        timer: 1500
                    });
                }
            });
        });
    });
</script>